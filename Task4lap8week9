#include <iostream>

using namespace std;

int main()
{
int s,non;
cin>>s;
int ar[s];
for(int i=0;i<s;i++)
cin>>ar[i];
for(int i=0;i<s-1;i++){
    for(int j=1;j<s;j++){
        
        if(ar[i]>ar[j]){
            non=ar[i];
            ar[i]=ar[j];
            ar[j]=non;
        } 
          
    }
    
    
}

for(int i:ar)
cout<<i<<' ';


    return 0;
}
