#include<iostream>
#include <string>
#include <cstring>
using namespace std;
int main() {
    string inpt;
    cout<<"enter your full name: ";
    getline(cin,inpt);
    for(int i=0;i<inpt.length();i++){
        cout<<char(toupper(inpt[i]));
    }
    return 0;
}
