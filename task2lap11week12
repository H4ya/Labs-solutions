#include<iostream>
#include <string>
#include <cstring>
using namespace std;
int main() {
    
    string inpt;
    cin>>inpt;
    for(char c:inpt){
      if(islower(c))
        cout<<char(toupper(c));
      else
        cout<<char(tolower(c));
    }
    return 0;
}
